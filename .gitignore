/public/jscontent/
/public/nusd/
/public/usernote/
/public/uploadquestion/
/public/prismpics/
/sessions/
/log/
/syncserver/
/.env
/spam/
# /scripts/backup_elcuemath.sh


#secret files
/scripts/drivebackup/token.json
/scripts/drivebackup/credentials.json

#ssl
/keys/

node_modules/
venv/
# / 가 앞에 있어야 루트 디렉토리 
#  앞에 / 가 없으면 매치되는 모든것 ignore
# 뒤에 /가 있으면 폴더를 의미. 
# 뒤에 /가 없으면 파일이나 폴더를 의미
